$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: Lightbox
status: experimental
group: ui
props:
  required:
    - uri
  properties:
    uri:
      title: Image URI
      type: string
    size:
      title: Image size
      type: string
slots:
  thumbnail:
    title: Thumbnail