$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: SVG icon
status: experimental
group: ui
props:
  type: object
  required:
    - icon
  properties:
    icon:
      title: Icon name
      type: string
      enum:
        - forum
        - search
        - share
        - toc
