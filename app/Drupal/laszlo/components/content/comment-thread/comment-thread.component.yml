$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: Comment thread
status: experimental
group: content
props:
  type: object
  required:
    - depth
  properties:
    depth:
      title: Thread depth (level)
      type: integer
    thread_id:
      title: Thread ID
      type: string
slots:
  comments:
    title: Comments in thread